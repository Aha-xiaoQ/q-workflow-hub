# Source and Runtime

- Canonical source: `<starter>/skills/q-pixel-art-creation`
- Runtime mirror: `<codex-home>/skills/q-pixel-art-creation`
- Personal bootstrap mirror: `<private-hub>/bootstrap/skills/q-pixel-art-creation`
- Initial lifecycle: `candidate`
- Sync direction for this creation: canonical source -> runtime after source
  validation. Future edits must compare both surfaces before choosing a base.
- This bundled lab candidate is not a stable domain release. Private training
  artifacts and maintainer review evidence are not distributed.
- The personal bootstrap mirror is a recovery copy, not the source of truth.
  Sync remains canonical source -> runtime and bootstrap after source
  validation. Hash parity is required for every maintained file.
- Compatibility: additive; it does not replace `q-game-canvas-iteration`,
  `imagegen`, or any legacy alias.
