# 助手帮助

创建日期：{{DATE}}

这是 {{WORKFLOW_LABEL}} 的简明帮助兜底。优先打开本地 HTML 入门页：
`{{WORKFLOW_HUB_PATH}}\FIRST_RUN_GUIDE.zh-CN.html`。

## 常用指令

- `{{USER_NAME}}`：做一次轻量恢复检查，确认工作流已经接入。
- `继续我的项目`：恢复当前项目或最可能的项目。
- `继续 <项目名>`：恢复指定项目。
- `状态`：总结当前焦点、阻塞点、改动文件和下一步。
- `保存状态` / `checkpoint`：切换或暂停前写入可恢复状态。
- `TODO`：查看轻量待办。
- `TODO：<内容>`：记录轻量提醒，不启动完整项目流程。
- `帮助` / `help`：打开或概括入门帮助。
- `工作流体检` / `体检`：当规则、状态或恢复流程显得臃肿、过期、不一致时，做更深检查。
- `人介入测试`：运行可复用的人参与测试流程，入口是 `scripts/human-loop-test-runner.py`。
- `登记项目`：预览项目摘要、代表性硬件/软件/文档线索和写入计划；确认后写入 `PROJECT_REGISTRY.md`。

## 工作流应该做什么

- 从文件恢复，而不是只依赖聊天记录。
- 恢复前优先核对最后几条聊天记录，避免旧指针覆盖用户最新纠正。
- 项目事实留在项目仓库里。
- 个人偏好、跨项目路由和当前状态留在个人 workflow hub。
- 可复用行为沉淀到 skill、脚本、测试或专家审查流程。
- 修改文件、运行验证、提交和推送前说明目标、证据和风险；远端推送、公开同步、破坏性清理、凭据处理、大范围安装或客户交付前先询问。

## 常用能力

- 快速恢复：通过 `ACTIVE_WORK.md`、项目状态文件和最近聊天证据定位当前焦点。
- 项目管理：登记项目、创建标准目录、维护 `PROJECT_REGISTRY.md` 和项目说明。
- Git 交付：检查状态、区分本地/远端一致性，并在推送前拉取或核对远端。
- 格式与编码卫生：检查 Markdown、模板、生成文件、乱码、旧标记、长问号串和 source/runtime 漂移。
- PPT 与视觉审查：制作、迁移、检查 PPT，避免重叠、截断、非正常换行和版式漂移。
- HTML 帮助/表单：优先用本地 HTML 作为用户可感知的帮助、配置和审阅界面。
- 专家协同：复杂任务可组合资料查询、专家审查、自动化验证和规则沉淀。

## 可用技能入口

- `q-workflow`：项目恢复、登记、结构、Git 交付和工作流状态。
- `q-assistant-profile`：个人协作偏好、快速恢复和轻量命令。
- `q-skill-creation`：创建、更新、审查、同步和发布 skill。
- `q-workflow`：工作流恢复、文本编码与生成面一致性检查。
- `q-ppt-creation` / `q-ppt-visual-review`：PPT 生成和视觉审查。
- `q-research-discovery` / `q-skill-pattern-learning`：外部资料、竞品/优秀流程学习和机制沉淀。

## 本地补充

如果 {{USER_NAME}} 希望定制帮助表面，可以在这里追加简短说明；更完整的首次使用说明应维护在 HTML 入门页。
