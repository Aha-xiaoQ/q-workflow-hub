# 更新记录

[English](CHANGELOG.md) | **简体中文**

q-workflow-hub 的所有重要更改都会记录在本文件中。

在创建稳定版发布标签后，本项目遵循语义化版本规范。

## 未发布

### 双语用户文档 — 2026-09-11

- 所有用户指南均提供英文和简体中文版本，加入双向语言切换和按任务组织的文档导航。
- 安装表单、验证消息和生成的提示词支持双语；切换语言时保留自定义填写内容。
- 将重复位置的指南统一指向完整文档，并加入语言配对、本地链接和浏览器交互自动检查。

### v1.2

- 添加简体中文 README，更新英文入口，并复用当前像素 Q 标志。
  在项目叙事技能中加入可调整的双语 README 标准与起步模板；
  将安装后指南的更新链接对齐到当前软件包。安装行为不变。
- 按任务需要选择技能流程：聚焦的 PDF／转录任务不再要求无关的提取包或服务商基准测试；
  保留证据、隐私、授权与独立发布审查检查。
- 将审计中的长度／普通换行混用问题设为建议项，验证显式根目录，
  并要求证明回滚故障注入确实执行到了预期位置。
- 保留对受众重要的限制说明与必需的许可声明。
- 添加根据能力调整的核心执行方式，以及绑定具体项目的发布验证。
- 隔离显式限定范围的技能组合审计；保留 v1.1 安全回归测试。
- 参阅 [v1.2 发布说明](docs/releases/q-workflow-v1.2.zh-CN.md)。

### 九月维护

- 保留 Bilibili 多分 P 选择，复用已授权的本地字幕获取路径，
  区分登录失败与字幕缺失，并提供离线回归测试。
- 添加退出已保存路径的选项，并隔离显式指定的路径注册表。
- 记录默认保持私有的资产晋升规则。

参阅[维护说明](docs/releases/maintenance-2026-09-06.zh-CN.md)。

## 1.1.0-beta - 2026-09-06

显示版本：`v1.1-beta`。范围与迁移限制请参阅发布说明。

- 加强恢复指针的可见性和验证契约。
- 添加有界的试点状态跟踪，并显式检查探测是否成功。
- 修复筛选后任务列表的验证，以及终止状态的接续保护。
- 扩充技能生命周期／评估指南与回归测试覆盖。
- 改善随包技能元数据的可移植性。
- 安装所有已注册的随包技能，并初始化空 TODO 的显示元数据。
- 支持从空闲工作流中心注册首个任务，并保留原始 TODO 名称。
- 通过显式路径初始化新的运行时镜像，不替换已有镜像。
- 添加两轮公开安装检查，包括事务式更新工具回滚。

请使用快速开始中的更新流程刷新已有安装。

## 1.0.0-beta - 2026-08-12

此公开 beta 以显示标签 `v1.0-beta` 发布。它引入以恢复能力为先的工作流内核，
同时保留现有起步包和旧版指针路径。

### q-workflow 内核

- 添加默认只读的 `q_workflow_manager.py` 入口，用于状态、诊断、组件检查、
  注册表验证及事务式任务状态操作。
- 添加版本化任务记录、只追加的权威事件链、比较并交换式规划、操作系统锁、
  原子替换、回读验证、回滚和幂等应用凭证。
- 添加严格的组件注册表，以及受注册表检查约束的引导副本／运行时同步。
- 通过稳定的状态、`elapsed_ms` 和 `failure_class` 字段提供最小运行证据，
  不存储提示词或私人产物正文。
- 加强恢复权威、运行时清单、发布证据、用户可见输出预检、技能组合根目录解析，
  以及独立专家交接／模型路由检查。

### 兼容性与发布状态

- 现有 Markdown 状态与 `RECOVERY_POINTER v1` 仍可读取。
- `ACTIVE_WORK.md` 仍是选定的当前焦点视图；已注册任务记录是实质性任务的权威来源，
  `sync_state` 仍是派生的兼容别名。
- 这是公开 beta，不是稳定 v1 声明。五个已注册核心组件受到发布检查约束；
  实验组件不在 beta 稳定性保证范围内。
- 由于尚未实现持久化预写日志，在多个文件切换之间强制终止进程，仍可能留下未完成的事务。

- 添加 `q-game-canvas-iteration` 作为试点通用技能，用于原创本地 Canvas／浏览器游戏反馈循环，
  包含源码／运行时权威管理和场景。经确认后停用初始 `game-canvas-iteration` 文件夹别名；
  旧短语仍保留为正式文本触发词。

- 收紧推广仓库推送频率：公司侧推广／共享仓库默认不因日常同步而推送，
  推送前需要明确的版本更新、稳定发布或重大更新请求。
- 添加可复用人工反馈测试 TC-15，用于处理权限拒绝，重点检查用户可见的安全继续执行说明与本地替代方案。
- 调整人工评审卡片，以被测提示词和用户可见的助手输出开头，将机器证据放入单独的结果／原始文件。
- 添加可复用人工反馈测试 TC-14，验证中文项目注册写入，包括注册表写入、解析器、不复制和不推送检查。
- 添加可复用人工反馈测试 TC-12，用于中文安装和首次恢复；同时提供中文 `ASSISTANT_HELP.md` 模板，
  避免中文安装得到英文或乱码帮助界面。
- 添加默认 PPT 需求收集入口，使未附详情的 PPT 制作请求先打开随包 HTML 表单；
  用户仍可在导出 JSON 后说 `开始制作`，或直接在聊天中提供详情。
- 将 q-workflow 系列技能重命名为 `q-<domain>-<job>` 格式，包括 `q-assistant-profile`、
  `q-skill-creation`、`q-skill-pattern-learning`、`q-ppt-creation`、`q-ppt-visual-review`
  和 `q-project-storytelling`；添加技能命名规范参考，并让个人引导副本同步脚本移除已重命名的旧技能副本。
- 为 GitHub 交接添加显式的推送前拉取／变基规则，使 `push GitHub` 包括发布前的远程最新状态检查。
- 收紧 GitHub 交接指南：已知需要代理的环境应先通过代理拉取／变基，再通过代理推送，
  而不是先尝试直接 HTTPS。
- 明确公开 `q-video-intake`／`q-audio-intake` 入口，帮助用户区分字幕／视觉视频资料接收
  与 STT 服务商基准测试、闭环转录评分。
- 添加公开 `q-workflow` 整洁性与定位参考，使清理、技能适配和工作流差异性检查成为起步包的一部分。
- 扩展 `init-user.ps1`，将完整默认技能集复制到工作流引导副本和可选运行时安装中，
  而不是只安装少量技能。
- 在 q-assistant-profile 模板中添加快速恢复调用规则，让包含配置档案名称的提示词
  能生成精简的工作流恢复面板，而不是被当作普通问候。
- 修复 `init-user.ps1`，在展开模板前按 UTF-8 读取，避免中文提示词示例和恢复标题出现乱码。
- 添加工作流引导副本同步脚本并加强通用技能同步规则，以递归文件列表和哈希验证，
  而不是抽查，检查源码、运行时、引导副本、私有与 GitHub 镜像。
- 修复本地安装运行器，让网页表单提交能够正确传递 `init-user.ps1` 命名参数，
  展开 `%USERPROFILE%` 和 `~` 路径，并显示安装日志而不损坏 Quick Resume 标记。
- 添加 `setup-intake.html` 和可配置的 `WorkflowLabel` 安装参数，让新用户可在本地收集安装字段，
  并看到 `【q-workflow | Quick Resume】` 或 `【小Q工作流 | Quick Resume】` 等 Quick Resume 标记。
- 为生成的工作流中心添加首次使用可视化指南：英文安装使用 `FIRST_RUN_GUIDE.html`，
  中文安装使用 `FIRST_RUN_GUIDE.zh-CN.html`。
- 添加 `q-skill-pattern-learning`，用于研究外部／内部技能、提取可迁移的工作流机制、
  在独立场景中验证经验，并决定是否将其纳入 q-workflow。
- 添加通用 `q-ppt-creation` 和 `q-ppt-visual-review` 技能，用于演示文稿制作、迁移、视觉审查和 PowerPoint 验证。
- 添加 q-workflow 实验契约与验证器，支持标准 `route -> plan -> edit -> validate -> review -> checkpoint` 任务流程。
- 添加 `fast mode`／`全速模式`，作为显式执行速度命令，同时保留安全检查。
- 添加命令别名层，覆盖帮助／状态／接续／检查点／规则同步和发布流程，
  提供中英文别名、有限的模糊匹配，以及完成前的通用技能同步审计。
- 将 `q-skill-creation` 的规则质量与工作流评估参考从公司工作流同步到公开起步包。
- 添加恢复加固规则，覆盖多仓库未提交更改的分类、恢复后的归纳整理、有限范围的运行时镜像同步及仓库命名治理。
- 添加公开 `repository-naming` 参考，并从 `q-workflow` 链接，用于重命名与注册表键审计。
- 在 `q-workflow` 和 q-assistant-profile 模板中添加 Quick Resume 恢复规则，
  包括在深入查找隐藏状态前检查未提交更改，以及明确报告首轮的正确性、资源与速度。
- 将快速开始精简为只关注安装，把日常使用指南移到安装后文档，缩短首次提示词，
  添加 Git 安装链接，记录中文接续提示词，并把安装检查移入智能体引导流程。
- 添加面向新手的上下文／数据／记忆边界、提示词／工作条目结构，以及工具／MCP 安全指南。
- 添加工作流中心工作条目简报模板，并让初始化程序将其复制到新的工作流中心。
- 在快速开始中添加首轮路线图，并使中文指南与首次运行验证评分一致。
- 将 Bilibili 新手教程仅记录为参考材料，并明确说明未复制转录文字、截图、资产或具有鲜明特征的文字。
- 添加精简的 q-video-intake 实用指南，汇总已验证路径、样本类别、视觉采样选择和完成规则。
- 添加明确的不确定性处理规则：未知事实必须标记为未知／未验证／需要重测，不能猜测后写入持久记录。
- 添加真实样本证据注册规则，让完成的音视频分析和基准测试样本在聊天压缩后仍可恢复。
- 添加 `q-video-intake/workflows/sample-registry.md`，并将其接入快速摘要、深入分析和视觉反向解析流程。
- 添加 `templates/SAMPLE_REGISTRY.md`，供需要受跟踪样本台账的项目使用。
- 明确用户指出缺失样本时，必须通过准确 URL、标题、创作者或短语匹配，
  才能将推断出的本地候选视为目标样本。
- 在 `q-audio-intake` 基准测试指南中添加持久样本记录要求，使 STT 服务商结论不依赖被忽略的本地输出。
- 明确被忽略的 `local-state` 输出是证据，而不是持久记忆；真实样本分析只有在结论持久记录后才算完成。
- 添加并行子智能体工作流规则，让长时间下载、基准测试、调研、验证和不重叠的修改
  能与主智能体工作同时进行，同时保留整合控制。
- 将持久紧急检查点的触发示例改为 ASCII 别名，避免可复用工作流文件中的编码漂移。
- 添加借助钩子捕捉经验的指南，以及 `q-skill-creation` 和 `q-research-discovery` 的显式路由规则。
- 添加平台压缩／会话失败恢复规则，让损坏的聊天窗口可以被放弃，
  新会话可从持久文件、截图和最新可见用户请求恢复工作。
- 添加压缩／会话不稳定时的紧急检查点触发词，包括 `紧急记录`、`保存现场`、`checkpoint` 和 `emergency save`。
- 修改 GitHub 同步指南：直接 HTTPS 不可靠时，默认优先使用临时本地代理，同时避免修改全局 Git 代理配置。
- 添加低摩擦授权方式和已完成状态接续规则，让智能体避免重复请求常规批准，也避免重新打开已完成工作。
- 添加技能可移植性指南，使本地技能实验可以被晋升或记录，以便跨机器恢复。
- 添加 `q-research-discovery` v1，用于定向调研规划、技能发现、卡住时的横向探索、来源评估和持久搜索日志。
- 根据实际自用经验改进 `q-research-discovery`，加入来源角色、采用决策、来源注册表模板，以及已记录的公开参考类别。
- 添加实验验证指南与评分量表，用于检查技能、工作流或文档模式是否在实践中有效。
- 添加 `q-audio-intake` 最小可用版本，用于音频优先的转录准备、OpenAI／Gemini STT 服务商入口和 CER／WER 转录评分。
- 为 `q-audio-intake` 添加 `whisper-cpp` 服务商支持，让本地 `whisper.cpp` 命令行构建，
  包括 Windows AMD／Vulkan 构建，也能使用同一套转录与评分流程进行基准测试。
- 为 `q-audio-intake` 添加可复现的 `whisper.cpp` 安装流程，覆盖稳定的本地路径、环境变量、
  CPU 冒烟测试配置、AMD／Vulkan 验证和闭环基准测试晋升。
- 添加闭环 STT 基准测试流程，以官方／平台字幕参考评估音频转录质量，而不是只主观查看转录内容。
- 在快速开始中添加首次运行验证评分，让新用户和智能体能通过可观察检查判断安装健康度。
- 增强 q-video-intake 字幕获取的稳健性：平台的一种字幕语言失败或被限流时，尝试其他字幕语言。
- 改进 q-video-intake 和 q-audio-intake 的 ffmpeg 检测，避免将 Windows 应用执行别名误认为可运行的 ffmpeg 二进制文件。
- 当字幕提取仍然成功时，将 `yt-dlp` JavaScript 运行时和请求模拟警告记录为不阻断流程的兼容性警告。
- 添加助手运行配置档案与角色模式钩子，支持稳定的助手行为和轻量的任务专属工作模式。
- 添加主动帮助和功能提示规则，让助手能在自然停顿点介绍有用的工作流能力并建议下一步。
- 添加暂停工作队列规则，使中断的任务能在后续工作完成后作为接续选项提供。
- 添加记忆与效率规则，涵盖自动经验记录、渐进式上下文读取、可恢复检查点节奏和 GitHub 代理回退。
- 添加 `PRINCIPLES.md`，记录 q-workflow 可靠性优先、友好和重视许可的产品原则。
- 添加 `q-skill-creation`，用于创建、更新和审查可靠且兼容 q-workflow 的技能。
- 更新生成的助手配置档案，优先用中文与小Q讨论，并保留可靠性优先的协作风格。
- 添加首个 `q-video-intake` 可复用技能，用于字幕优先的音视频资料接收、
  可选 OpenAI 转录回退，以及教程分析笔记。
- 添加 q-video-intake 分析包：转录分块、可复用分析提示词，以及处理已有转录的 `analyze` 命令。
- 添加借鉴成熟视频摘要技能组织思路的 q-video-intake 工作流路由文档：转录提取、快速摘要和深入分析。
- 添加 q-video-intake 视觉反向解析帧包，用于在添加模型适配器之前采样本地视频帧，
  并准备视觉模型／人工审查提示词。
- 添加 q-video-intake 视觉笔记模板，让帧包无需模型适配器即可直接进入结构化 UI／动作／OCR 审查。
- 添加 q-video-intake `visual-analyze`，为采样帧包提供适合服务商调用的视觉提示词和可选 GPT 视觉调用。
- 添加 q-video-intake `visual-agent-task`，使 Codex／GPT 会话在 API 访问受阻时，也能根据帧包生成结构化视觉笔记。
- 添加 q-video-intake `visual-score`，按检查表将视觉笔记与预期标签、事实及帧观察结果进行回归评分。
- 为 q-video-intake `visual-analyze` 添加 Azure OpenAI 支持，让 Azure 密钥使用 Azure 端点和部署名称，
  而不是标准 OpenAI API。
- 改进 Windows ffmpeg 检测，使通过 PowerShell 解析到的 ffmpeg 安装可用于本地视频和音频回退。
- 添加第三方参考资料与许可合规说明，为后续技能集成提供依据。
- 改进 Bilibili 字幕需要登录或浏览器 Cookie 无法复制时，q-video-intake 的下一步指引。
- 为 q-video-intake 添加 `--cookies-file` 支持，在浏览器 Cookie 复制或 DPAPI 解密失败时，
  可使用导出的 Netscape 格式 Cookie 文件。
- 添加 Bilibili AI 字幕语言默认值和按请求顺序的字幕选择，在有中文 AI 字幕时优先于英文。
- 验证真实 Bilibili 字幕提取，覆盖导出的 Cookie、Bilibili AI 字幕语言和中文转录选择。
- 添加面向用户的快速开始指南，包含安装前置条件、首次提示词、生成文件、日常提示词和排错说明。
- 添加中文快速开始与首次提示词文档，支持双语新手引导。
- 扩展环境预检，覆盖 Git、PowerShell、网络访问、可写工作区路径和可选工作流中心远程仓库。
- 添加面向新手的 Codex、Claude Code 及可选 CC Switch 安装路线指南。
- 围绕适合新手的顺畅路径、检查点和成功标准，重写快速开始的新手引导。
- 添加首次运行“先跑起来”路径与正常智能体对话顺序，让新用户知道粘贴提示词后应该发生什么。
- 将生成的模板文件改为无 BOM 的 UTF-8，确保生成的技能文件能被基于前置元数据的技能加载器正常读取。
- 更新首次运行指南，要求智能体收集安装字段后、修改文件前，先解释工作流。
- 加强恢复行为：初始化程序保留现有个人注册表和当前工作文件；
  接续指令要求先进行恢复检查，才能断定没有正在进行的工作。
- 修复公开扫描对安全传递 API 密钥变量的误报，以及 PowerShell `$Matches` 变量冲突。

## 0.1.0 - 2026-06-05

- 建立首个公开起步仓库。
- 添加 `q-workflow` 技能。
- 添加工作流中心与项目记忆模板。
- 添加 Windows PowerShell 初始化程序。
- 添加首次运行智能体提示词与机器引导指南。
